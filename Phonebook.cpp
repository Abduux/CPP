#include "Phonebook.hpp"



int Phonebook::AddContact()
{
    //loop on te informations and save them if valid 
}

void Phonebook::SearchContact()
{
    int i = 0;
    while(i < index)
    {
        std::
    }
    // display all contacts
    // and let the user pick a number to display the 
}

void Phonebook::Exit()
{
    exit(0);
}